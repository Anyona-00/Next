import React from 'react';

function Loading() {
  // This is where you define the loading page's UI using JSX.
  return <div>Loading...</div>;
}

export default Loading; // Make sure the component is correctly exported.